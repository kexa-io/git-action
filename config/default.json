{
    "http":[
        {
            "description": "main end point for the api 4urcloud",
            "prefix": "DEMO-",
            "rules": [
                "Deployement"
            ],
            "METHOD": "GET",
            "URL": "https://api.4urcloud.eu/"
        },
        {
            "description": "main end point for the none api to test error",
            "prefix": "DEMO-",
            "rules": [
                "Deployement"
            ],
            "METHOD": "GET",
            "URL": "https://google.com/"
        }
    ],
    "kubernetes":[
        {
            "description": "kubernetes global innovtech",
            "prefix": "DEMO-",
            "rules": [
                "Deployement"
            ]
        }
    ],
    "github": [
        {
            "description": "github global innovtech",
            "prefix": "DEMO-",
            "rules": [
                "Deployement"
            ]
        }
    ]
}
{
    "kubernetes": [
        {
            "Description": "Main kubernetes cluster",
            "prefix": "DEMO-",
            "rules": [
                "Deployement"
            ]
        }
    ],
    "http":[
        {
            "Description": "main end point for the api 4urcloud",
            "prefix": "DEMO-",
            "rules": [
                "Deployement"
            ],
            "METHOD": "GET",
            "URL": "https://api.4urcloud.eu/"
        }
    ],
    "github":[
        {
            "Description": "Organization 4urcloud",
            "prefix": "DEMO-",
            "rules": [
                "Deployement"
            ]
        }
    ]
}
import { BackupStatus } from "../operationsInterfaces";
import { RecoveryServicesBackupClient } from "../recoveryServicesBackupClient";
import { BackupStatusRequest, BackupStatusGetOptionalParams, BackupStatusGetResponse } from "../models";
/** Class containing BackupStatus operations. */
export declare class BackupStatusImpl implements BackupStatus {
    private readonly client;
    /**
     * Initialize a new instance of the class BackupStatus class.
     * @param client Reference to the service client
     */
    constructor(client: RecoveryServicesBackupClient);
    /**
     * Get the container backup status
     * @param azureRegion Azure region to hit Api
     * @param parameters Container Backup Status Request
     * @param options The options parameters.
     */
    get(azureRegion: string, parameters: BackupStatusRequest, options?: BackupStatusGetOptionalParams): Promise<BackupStatusGetResponse>;
}
//# sourceMappingURL=backupStatus.d.ts.map
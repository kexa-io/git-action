import { PagedAsyncIterableIterator } from "@azure/core-paging";
import { RestorableMongodbDatabases } from "../operationsInterfaces";
import { CosmosDBManagementClient } from "../cosmosDBManagementClient";
import { RestorableMongodbDatabaseGetResult, RestorableMongodbDatabasesListOptionalParams } from "../models";
/** Class containing RestorableMongodbDatabases operations. */
export declare class RestorableMongodbDatabasesImpl implements RestorableMongodbDatabases {
    private readonly client;
    /**
     * Initialize a new instance of the class RestorableMongodbDatabases class.
     * @param client Reference to the service client
     */
    constructor(client: CosmosDBManagementClient);
    /**
     * Show the event feed of all mutations done on all the Azure Cosmos DB MongoDB databases under the
     * restorable account.  This helps in scenario where database was accidentally deleted to get the
     * deletion time.  This API requires
     * 'Microsoft.DocumentDB/locations/restorableDatabaseAccounts/.../read' permission
     * @param location Cosmos DB region, with spaces between words and each word capitalized.
     * @param instanceId The instanceId GUID of a restorable database account.
     * @param options The options parameters.
     */
    list(location: string, instanceId: string, options?: RestorableMongodbDatabasesListOptionalParams): PagedAsyncIterableIterator<RestorableMongodbDatabaseGetResult>;
    private listPagingPage;
    private listPagingAll;
    /**
     * Show the event feed of all mutations done on all the Azure Cosmos DB MongoDB databases under the
     * restorable account.  This helps in scenario where database was accidentally deleted to get the
     * deletion time.  This API requires
     * 'Microsoft.DocumentDB/locations/restorableDatabaseAccounts/.../read' permission
     * @param location Cosmos DB region, with spaces between words and each word capitalized.
     * @param instanceId The instanceId GUID of a restorable database account.
     * @param options The options parameters.
     */
    private _list;
}
//# sourceMappingURL=restorableMongodbDatabases.d.ts.map
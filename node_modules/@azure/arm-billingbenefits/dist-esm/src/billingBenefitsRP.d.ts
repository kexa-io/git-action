import * as coreClient from "@azure/core-client";
import * as coreAuth from "@azure/core-auth";
import { Operations, SavingsPlanOrderAlias, SavingsPlanOrder, SavingsPlan, ReservationOrderAlias } from "./operationsInterfaces";
import { BillingBenefitsRPOptionalParams, SavingsPlanPurchaseValidateRequest, ValidatePurchaseOptionalParams, ValidatePurchaseResponse } from "./models";
export declare class BillingBenefitsRP extends coreClient.ServiceClient {
    $host: string;
    apiVersion: string;
    /**
     * Initializes a new instance of the BillingBenefitsRP class.
     * @param credentials Subscription credentials which uniquely identify client subscription.
     * @param options The parameter options
     */
    constructor(credentials: coreAuth.TokenCredential, options?: BillingBenefitsRPOptionalParams);
    /** A function that adds a policy that sets the api-version (or equivalent) to reflect the library version. */
    private addCustomApiVersionPolicy;
    /**
     * Validate savings plan purchase.
     * @param body Request body for validating the purchase of a savings plan
     * @param options The options parameters.
     */
    validatePurchase(body: SavingsPlanPurchaseValidateRequest, options?: ValidatePurchaseOptionalParams): Promise<ValidatePurchaseResponse>;
    operations: Operations;
    savingsPlanOrderAlias: SavingsPlanOrderAlias;
    savingsPlanOrder: SavingsPlanOrder;
    savingsPlan: SavingsPlan;
    reservationOrderAlias: ReservationOrderAlias;
}
//# sourceMappingURL=billingBenefitsRP.d.ts.map
"use strict";
/**
 * Created by donisaac on 23/12/2022
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.createTypedefCode = void 0;
const prettier = require("prettier");
const json_to_ts_1 = require("json-to-ts");
const createTypedefCode = (config) => {
    const rootName = 'IConfig';
    const ts = prettier.format([
        '/* tslint:disable */',
        '/* eslint-disable */',
        'declare module "node-config-ts" {'
    ]
        .concat((0, json_to_ts_1.default)(config, { rootName }))
        .concat([
        'export const config: Config',
        `export type Config = ${rootName}`,
        '}'
    ])
        .join('\n'), {
        parser: 'typescript',
        semi: false
    });
    return ts;
};
exports.createTypedefCode = createTypedefCode;

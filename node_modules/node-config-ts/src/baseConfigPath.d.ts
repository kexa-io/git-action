type NonConfigTSEnv = {
    env: {
        NODE_CONFIG_TS_DIR?: string;
        NODE_ENV?: string;
        NODE_CONFIG_TS_ENV?: string;
    };
};
/**
 * Returns the base path for loading the configurations.
 */
export declare const baseConfigPath: <T extends NonConfigTSEnv>(process: T) => string;
export {};

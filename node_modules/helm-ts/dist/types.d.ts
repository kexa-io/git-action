export declare type GlobalFlags = {
    namespace?: string;
};
export declare type ListFlags = GlobalFlags & {
    allNamespaces?: boolean;
};
export declare type InstallFlags = GlobalFlags & {};
export declare type Release = {
    name: string;
    namespace: string;
    revision: string;
    updated: string;
    status: 'unknown' | 'deployed' | 'uninstalled' | 'superseded' | 'failed' | 'uninstalling' | 'pending-install' | 'pending-upgrade' | 'pending-rollback';
    chart: string;
    'app_version': string;
};
export declare type Repo = {
    name: string;
    url: string;
};
export declare type RepoAddFlags = GlobalFlags & {
    username?: string;
    password?: string;
};
export declare type RepoListFlags = GlobalFlags & {};

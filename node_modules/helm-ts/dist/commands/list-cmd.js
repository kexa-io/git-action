"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const helm_cmd_1 = require("./helm-cmd");
const helpers_1 = require("../utils/helpers");
const getAliasedListFlags = ({ allNamespaces = false, } = {}) => ({
    '--all-namespaces': allNamespaces,
});
const buildHelmListCmd = (flags) => {
    const allFlags = Object.assign(Object.assign({}, helm_cmd_1.getAliasedGlobalFlags(flags)), getAliasedListFlags(flags));
    const flagsString = helpers_1.buildFlagsString(allFlags);
    return `helm list -o json ${flagsString}`;
};
const list = (flags = {}) => __awaiter(void 0, void 0, void 0, function* () {
    const command = buildHelmListCmd(flags);
    const stdout = yield helpers_1.runCommand(command);
    return JSON.parse(stdout);
});
exports.default = list;
//# sourceMappingURL=list-cmd.js.map
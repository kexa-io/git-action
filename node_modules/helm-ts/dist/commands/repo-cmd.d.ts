import { Repo, RepoAddFlags } from '../types';
declare const _default: {
    add: (name: string, url: string, flags?: RepoAddFlags) => Promise<string>;
    list: (flags?: import("../types").GlobalFlags) => Promise<Repo[]>;
};
export default _default;

import { getEndpointPlugin } from "@smithy/middleware-endpoint";
import { getSerdePlugin } from "@smithy/middleware-serde";
import { Command as $Command } from "@smithy/smithy-client";
import { commonParams } from "../endpoint/EndpointParameters";
import { CreateStudioRequestFilterSensitiveLog, CreateStudioResponseFilterSensitiveLog, } from "../models/models_0";
import { de_CreateStudioCommand, se_CreateStudioCommand } from "../protocols/Aws_restJson1";
export { $Command };
export class CreateStudioCommand extends $Command
    .classBuilder()
    .ep({
    ...commonParams,
})
    .m(function (Command, cs, config, o) {
    return [
        getSerdePlugin(config, this.serialize, this.deserialize),
        getEndpointPlugin(config, Command.getEndpointParameterInstructions()),
    ];
})
    .s("nimble", "CreateStudio", {})
    .n("NimbleClient", "CreateStudioCommand")
    .f(CreateStudioRequestFilterSensitiveLog, CreateStudioResponseFilterSensitiveLog)
    .ser(se_CreateStudioCommand)
    .de(de_CreateStudioCommand)
    .build() {
}

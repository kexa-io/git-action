import { createPaginator } from "@smithy/core";
import { ListStudioComponentsCommand, } from "../commands/ListStudioComponentsCommand";
import { NimbleClient } from "../NimbleClient";
export const paginateListStudioComponents = createPaginator(NimbleClient, ListStudioComponentsCommand, "nextToken", "nextToken", "maxResults");

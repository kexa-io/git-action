import { createPaginator } from "@smithy/core";
import { ListDevicesJobsCommand, } from "../commands/ListDevicesJobsCommand";
import { PanoramaClient } from "../PanoramaClient";
export const paginateListDevicesJobs = createPaginator(PanoramaClient, ListDevicesJobsCommand, "NextToken", "NextToken", "MaxResults");

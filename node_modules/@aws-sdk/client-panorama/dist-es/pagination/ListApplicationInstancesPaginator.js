import { createPaginator } from "@smithy/core";
import { ListApplicationInstancesCommand, } from "../commands/ListApplicationInstancesCommand";
import { PanoramaClient } from "../PanoramaClient";
export const paginateListApplicationInstances = createPaginator(PanoramaClient, ListApplicationInstancesCommand, "NextToken", "NextToken", "MaxResults");

import { AwsSdkSigV4AuthInputConfig, AwsSdkSigV4AuthResolvedConfig, AwsSdkSigV4PreviouslyResolved } from "@aws-sdk/core";
import { HandlerExecutionContext, HttpAuthScheme, HttpAuthSchemeParameters, HttpAuthSchemeParametersProvider, HttpAuthSchemeProvider } from "@smithy/types";
import { ACMPCAClientResolvedConfig } from "../ACMPCAClient";
/**
 * @internal
 */
export interface ACMPCAHttpAuthSchemeParameters extends HttpAuthSchemeParameters {
    region?: string;
}
/**
 * @internal
 */
export interface ACMPCAHttpAuthSchemeParametersProvider extends HttpAuthSchemeParametersProvider<ACMPCAClientResolvedConfig, HandlerExecutionContext, ACMPCAHttpAuthSchemeParameters, object> {
}
/**
 * @internal
 */
export declare const defaultACMPCAHttpAuthSchemeParametersProvider: (config: ACMPCAClientResolvedConfig, context: HandlerExecutionContext, input: object) => Promise<ACMPCAHttpAuthSchemeParameters>;
/**
 * @internal
 */
export interface ACMPCAHttpAuthSchemeProvider extends HttpAuthSchemeProvider<ACMPCAHttpAuthSchemeParameters> {
}
/**
 * @internal
 */
export declare const defaultACMPCAHttpAuthSchemeProvider: ACMPCAHttpAuthSchemeProvider;
/**
 * @internal
 */
export interface HttpAuthSchemeInputConfig extends AwsSdkSigV4AuthInputConfig {
    /**
     * experimentalIdentityAndAuth: Configuration of HttpAuthSchemes for a client which provides default identity providers and signers per auth scheme.
     * @internal
     */
    httpAuthSchemes?: HttpAuthScheme[];
    /**
     * experimentalIdentityAndAuth: Configuration of an HttpAuthSchemeProvider for a client which resolves which HttpAuthScheme to use.
     * @internal
     */
    httpAuthSchemeProvider?: ACMPCAHttpAuthSchemeProvider;
}
/**
 * @internal
 */
export interface HttpAuthSchemeResolvedConfig extends AwsSdkSigV4AuthResolvedConfig {
    /**
     * experimentalIdentityAndAuth: Configuration of HttpAuthSchemes for a client which provides default identity providers and signers per auth scheme.
     * @internal
     */
    readonly httpAuthSchemes: HttpAuthScheme[];
    /**
     * experimentalIdentityAndAuth: Configuration of an HttpAuthSchemeProvider for a client which resolves which HttpAuthScheme to use.
     * @internal
     */
    readonly httpAuthSchemeProvider: ACMPCAHttpAuthSchemeProvider;
}
/**
 * @internal
 */
export declare const resolveHttpAuthSchemeConfig: <T>(config: T & HttpAuthSchemeInputConfig & AwsSdkSigV4PreviouslyResolved) => T & HttpAuthSchemeResolvedConfig;

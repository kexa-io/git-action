import { createPaginator } from "@smithy/core";
import { ListStreamsCommand } from "../commands/ListStreamsCommand";
import { KinesisVideoClient } from "../KinesisVideoClient";
export const paginateListStreams = createPaginator(KinesisVideoClient, ListStreamsCommand, "NextToken", "NextToken", "MaxResults");

import { createPaginator } from "@smithy/core";
import { ListSignalingChannelsCommand, } from "../commands/ListSignalingChannelsCommand";
import { KinesisVideoClient } from "../KinesisVideoClient";
export const paginateListSignalingChannels = createPaginator(KinesisVideoClient, ListSignalingChannelsCommand, "NextToken", "NextToken", "MaxResults");

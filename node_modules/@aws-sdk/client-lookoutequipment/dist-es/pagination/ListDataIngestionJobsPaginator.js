import { createPaginator } from "@smithy/core";
import { ListDataIngestionJobsCommand, } from "../commands/ListDataIngestionJobsCommand";
import { LookoutEquipmentClient } from "../LookoutEquipmentClient";
export const paginateListDataIngestionJobs = createPaginator(LookoutEquipmentClient, ListDataIngestionJobsCommand, "NextToken", "NextToken", "MaxResults");

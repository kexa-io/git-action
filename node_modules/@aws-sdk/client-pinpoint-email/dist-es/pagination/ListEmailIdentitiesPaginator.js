import { createPaginator } from "@smithy/core";
import { ListEmailIdentitiesCommand, } from "../commands/ListEmailIdentitiesCommand";
import { PinpointEmailClient } from "../PinpointEmailClient";
export const paginateListEmailIdentities = createPaginator(PinpointEmailClient, ListEmailIdentitiesCommand, "NextToken", "NextToken", "PageSize");

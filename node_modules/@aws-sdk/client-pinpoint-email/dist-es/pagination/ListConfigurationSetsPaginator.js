import { createPaginator } from "@smithy/core";
import { ListConfigurationSetsCommand, } from "../commands/ListConfigurationSetsCommand";
import { PinpointEmailClient } from "../PinpointEmailClient";
export const paginateListConfigurationSets = createPaginator(PinpointEmailClient, ListConfigurationSetsCommand, "NextToken", "NextToken", "PageSize");

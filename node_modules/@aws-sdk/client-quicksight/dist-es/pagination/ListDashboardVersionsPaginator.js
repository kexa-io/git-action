import { createPaginator } from "@smithy/core";
import { ListDashboardVersionsCommand, } from "../commands/ListDashboardVersionsCommand";
import { QuickSightClient } from "../QuickSightClient";
export const paginateListDashboardVersions = createPaginator(QuickSightClient, ListDashboardVersionsCommand, "NextToken", "NextToken", "MaxResults");

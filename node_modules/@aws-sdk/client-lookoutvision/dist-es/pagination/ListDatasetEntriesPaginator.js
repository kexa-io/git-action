import { createPaginator } from "@smithy/core";
import { ListDatasetEntriesCommand, } from "../commands/ListDatasetEntriesCommand";
import { LookoutVisionClient } from "../LookoutVisionClient";
export const paginateListDatasetEntries = createPaginator(LookoutVisionClient, ListDatasetEntriesCommand, "NextToken", "NextToken", "MaxResults");

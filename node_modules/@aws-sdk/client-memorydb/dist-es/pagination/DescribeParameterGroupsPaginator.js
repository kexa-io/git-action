import { createPaginator } from "@smithy/core";
import { DescribeParameterGroupsCommand, } from "../commands/DescribeParameterGroupsCommand";
import { MemoryDBClient } from "../MemoryDBClient";
export const paginateDescribeParameterGroups = createPaginator(MemoryDBClient, DescribeParameterGroupsCommand, "NextToken", "NextToken", "MaxResults");

import { createPaginator } from "@smithy/core";
import { DescribeSubnetGroupsCommand, } from "../commands/DescribeSubnetGroupsCommand";
import { MemoryDBClient } from "../MemoryDBClient";
export const paginateDescribeSubnetGroups = createPaginator(MemoryDBClient, DescribeSubnetGroupsCommand, "NextToken", "NextToken", "MaxResults");

import { createPaginator } from "@smithy/core";
import { BackupGatewayClient } from "../BackupGatewayClient";
import { ListVirtualMachinesCommand, } from "../commands/ListVirtualMachinesCommand";
export const paginateListVirtualMachines = createPaginator(BackupGatewayClient, ListVirtualMachinesCommand, "NextToken", "NextToken", "MaxResults");

import { createPaginator } from "@smithy/core";
import { DescribeLaunchConfigurationTemplatesCommand, } from "../commands/DescribeLaunchConfigurationTemplatesCommand";
import { MgnClient } from "../MgnClient";
export const paginateDescribeLaunchConfigurationTemplates = createPaginator(MgnClient, DescribeLaunchConfigurationTemplatesCommand, "nextToken", "nextToken", "maxResults");

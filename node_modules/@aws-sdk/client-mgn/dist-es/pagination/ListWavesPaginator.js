import { createPaginator } from "@smithy/core";
import { ListWavesCommand } from "../commands/ListWavesCommand";
import { MgnClient } from "../MgnClient";
export const paginateListWaves = createPaginator(MgnClient, ListWavesCommand, "nextToken", "nextToken", "maxResults");

import { createPaginator } from "@smithy/core";
import { ListSourceServerActionsCommand, } from "../commands/ListSourceServerActionsCommand";
import { MgnClient } from "../MgnClient";
export const paginateListSourceServerActions = createPaginator(MgnClient, ListSourceServerActionsCommand, "nextToken", "nextToken", "maxResults");

import { createPaginator } from "@smithy/core";
import { ListDiscoveredResourcesCommand, } from "../commands/ListDiscoveredResourcesCommand";
import { MigrationHubClient } from "../MigrationHubClient";
export const paginateListDiscoveredResources = createPaginator(MigrationHubClient, ListDiscoveredResourcesCommand, "NextToken", "NextToken", "MaxResults");

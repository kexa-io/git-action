import { createPaginator } from "@smithy/core";
import { ListDataSetRevisionsCommand, } from "../commands/ListDataSetRevisionsCommand";
import { DataExchangeClient } from "../DataExchangeClient";
export const paginateListDataSetRevisions = createPaginator(DataExchangeClient, ListDataSetRevisionsCommand, "NextToken", "NextToken", "MaxResults");

import { createPaginator } from "@smithy/core";
import { ListHarvestJobsCommand, } from "../commands/ListHarvestJobsCommand";
import { MediaPackageClient } from "../MediaPackageClient";
export const paginateListHarvestJobs = createPaginator(MediaPackageClient, ListHarvestJobsCommand, "NextToken", "NextToken", "MaxResults");

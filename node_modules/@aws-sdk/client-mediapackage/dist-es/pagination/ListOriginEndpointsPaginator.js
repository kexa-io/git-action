import { createPaginator } from "@smithy/core";
import { ListOriginEndpointsCommand, } from "../commands/ListOriginEndpointsCommand";
import { MediaPackageClient } from "../MediaPackageClient";
export const paginateListOriginEndpoints = createPaginator(MediaPackageClient, ListOriginEndpointsCommand, "NextToken", "NextToken", "MaxResults");

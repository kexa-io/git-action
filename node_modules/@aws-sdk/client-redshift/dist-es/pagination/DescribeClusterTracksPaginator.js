import { createPaginator } from "@smithy/core";
import { DescribeClusterTracksCommand, } from "../commands/DescribeClusterTracksCommand";
import { RedshiftClient } from "../RedshiftClient";
export const paginateDescribeClusterTracks = createPaginator(RedshiftClient, DescribeClusterTracksCommand, "Marker", "Marker", "MaxRecords");

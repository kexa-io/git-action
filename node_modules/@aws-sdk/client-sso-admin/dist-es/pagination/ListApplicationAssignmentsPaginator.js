import { createPaginator } from "@smithy/core";
import { ListApplicationAssignmentsCommand, } from "../commands/ListApplicationAssignmentsCommand";
import { SSOAdminClient } from "../SSOAdminClient";
export const paginateListApplicationAssignments = createPaginator(SSOAdminClient, ListApplicationAssignmentsCommand, "NextToken", "NextToken", "MaxResults");

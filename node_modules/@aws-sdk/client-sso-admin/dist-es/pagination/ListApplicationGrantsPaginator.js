import { createPaginator } from "@smithy/core";
import { ListApplicationGrantsCommand, } from "../commands/ListApplicationGrantsCommand";
import { SSOAdminClient } from "../SSOAdminClient";
export const paginateListApplicationGrants = createPaginator(SSOAdminClient, ListApplicationGrantsCommand, "NextToken", "NextToken", "");

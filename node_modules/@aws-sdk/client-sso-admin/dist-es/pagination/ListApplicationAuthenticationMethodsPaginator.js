import { createPaginator } from "@smithy/core";
import { ListApplicationAuthenticationMethodsCommand, } from "../commands/ListApplicationAuthenticationMethodsCommand";
import { SSOAdminClient } from "../SSOAdminClient";
export const paginateListApplicationAuthenticationMethods = createPaginator(SSOAdminClient, ListApplicationAuthenticationMethodsCommand, "NextToken", "NextToken", "");

import { createPaginator } from "@smithy/core";
import { ListTestGridSessionsCommand, } from "../commands/ListTestGridSessionsCommand";
import { DeviceFarmClient } from "../DeviceFarmClient";
export const paginateListTestGridSessions = createPaginator(DeviceFarmClient, ListTestGridSessionsCommand, "nextToken", "nextToken", "maxResult");

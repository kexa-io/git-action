import { createPaginator } from "@smithy/core";
import { ListTestGridProjectsCommand, } from "../commands/ListTestGridProjectsCommand";
import { DeviceFarmClient } from "../DeviceFarmClient";
export const paginateListTestGridProjects = createPaginator(DeviceFarmClient, ListTestGridProjectsCommand, "nextToken", "nextToken", "maxResult");

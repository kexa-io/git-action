import { createPaginator } from "@smithy/core";
import { CloudDirectoryClient } from "../CloudDirectoryClient";
import { ListManagedSchemaArnsCommand, } from "../commands/ListManagedSchemaArnsCommand";
export const paginateListManagedSchemaArns = createPaginator(CloudDirectoryClient, ListManagedSchemaArnsCommand, "NextToken", "NextToken", "MaxResults");

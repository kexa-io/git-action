import { createPaginator } from "@smithy/core";
import { CloudDirectoryClient } from "../CloudDirectoryClient";
import { ListAttachedIndicesCommand, } from "../commands/ListAttachedIndicesCommand";
export const paginateListAttachedIndices = createPaginator(CloudDirectoryClient, ListAttachedIndicesCommand, "NextToken", "NextToken", "MaxResults");

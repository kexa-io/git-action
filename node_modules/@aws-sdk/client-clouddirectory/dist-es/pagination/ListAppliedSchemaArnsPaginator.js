import { createPaginator } from "@smithy/core";
import { CloudDirectoryClient } from "../CloudDirectoryClient";
import { ListAppliedSchemaArnsCommand, } from "../commands/ListAppliedSchemaArnsCommand";
export const paginateListAppliedSchemaArns = createPaginator(CloudDirectoryClient, ListAppliedSchemaArnsCommand, "NextToken", "NextToken", "MaxResults");

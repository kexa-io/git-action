import { createPaginator } from "@smithy/core";
import { CloudDirectoryClient } from "../CloudDirectoryClient";
import { ListDevelopmentSchemaArnsCommand, } from "../commands/ListDevelopmentSchemaArnsCommand";
export const paginateListDevelopmentSchemaArns = createPaginator(CloudDirectoryClient, ListDevelopmentSchemaArnsCommand, "NextToken", "NextToken", "MaxResults");

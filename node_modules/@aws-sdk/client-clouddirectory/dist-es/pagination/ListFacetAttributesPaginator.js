import { createPaginator } from "@smithy/core";
import { CloudDirectoryClient } from "../CloudDirectoryClient";
import { ListFacetAttributesCommand, } from "../commands/ListFacetAttributesCommand";
export const paginateListFacetAttributes = createPaginator(CloudDirectoryClient, ListFacetAttributesCommand, "NextToken", "NextToken", "MaxResults");

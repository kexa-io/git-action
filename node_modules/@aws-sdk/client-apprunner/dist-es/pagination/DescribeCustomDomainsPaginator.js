import { createPaginator } from "@smithy/core";
import { AppRunnerClient } from "../AppRunnerClient";
import { DescribeCustomDomainsCommand, } from "../commands/DescribeCustomDomainsCommand";
export const paginateDescribeCustomDomains = createPaginator(AppRunnerClient, DescribeCustomDomainsCommand, "NextToken", "NextToken", "MaxResults");

import { createPaginator } from "@smithy/core";
import { ListLedgersCommand } from "../commands/ListLedgersCommand";
import { QLDBClient } from "../QLDBClient";
export const paginateListLedgers = createPaginator(QLDBClient, ListLedgersCommand, "NextToken", "NextToken", "MaxResults");

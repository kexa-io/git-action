import { createPaginator } from "@smithy/core";
import { ListEnvironmentOutputsCommand, } from "../commands/ListEnvironmentOutputsCommand";
import { ProtonClient } from "../ProtonClient";
export const paginateListEnvironmentOutputs = createPaginator(ProtonClient, ListEnvironmentOutputsCommand, "nextToken", "nextToken", "");

import { createPaginator } from "@smithy/core";
import { ListServiceInstancesCommand, } from "../commands/ListServiceInstancesCommand";
import { ProtonClient } from "../ProtonClient";
export const paginateListServiceInstances = createPaginator(ProtonClient, ListServiceInstancesCommand, "nextToken", "nextToken", "maxResults");

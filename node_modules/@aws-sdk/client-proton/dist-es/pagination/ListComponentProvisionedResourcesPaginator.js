import { createPaginator } from "@smithy/core";
import { ListComponentProvisionedResourcesCommand, } from "../commands/ListComponentProvisionedResourcesCommand";
import { ProtonClient } from "../ProtonClient";
export const paginateListComponentProvisionedResources = createPaginator(ProtonClient, ListComponentProvisionedResourcesCommand, "nextToken", "nextToken", "");

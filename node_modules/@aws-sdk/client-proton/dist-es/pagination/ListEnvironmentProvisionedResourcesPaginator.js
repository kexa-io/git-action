import { createPaginator } from "@smithy/core";
import { ListEnvironmentProvisionedResourcesCommand, } from "../commands/ListEnvironmentProvisionedResourcesCommand";
import { ProtonClient } from "../ProtonClient";
export const paginateListEnvironmentProvisionedResources = createPaginator(ProtonClient, ListEnvironmentProvisionedResourcesCommand, "nextToken", "nextToken", "");

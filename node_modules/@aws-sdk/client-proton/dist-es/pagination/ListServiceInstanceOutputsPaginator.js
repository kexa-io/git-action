import { createPaginator } from "@smithy/core";
import { ListServiceInstanceOutputsCommand, } from "../commands/ListServiceInstanceOutputsCommand";
import { ProtonClient } from "../ProtonClient";
export const paginateListServiceInstanceOutputs = createPaginator(ProtonClient, ListServiceInstanceOutputsCommand, "nextToken", "nextToken", "");

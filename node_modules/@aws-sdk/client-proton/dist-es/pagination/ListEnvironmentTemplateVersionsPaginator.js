import { createPaginator } from "@smithy/core";
import { ListEnvironmentTemplateVersionsCommand, } from "../commands/ListEnvironmentTemplateVersionsCommand";
import { ProtonClient } from "../ProtonClient";
export const paginateListEnvironmentTemplateVersions = createPaginator(ProtonClient, ListEnvironmentTemplateVersionsCommand, "nextToken", "nextToken", "maxResults");

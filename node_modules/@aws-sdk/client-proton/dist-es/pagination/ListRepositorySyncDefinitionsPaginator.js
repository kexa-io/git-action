import { createPaginator } from "@smithy/core";
import { ListRepositorySyncDefinitionsCommand, } from "../commands/ListRepositorySyncDefinitionsCommand";
import { ProtonClient } from "../ProtonClient";
export const paginateListRepositorySyncDefinitions = createPaginator(ProtonClient, ListRepositorySyncDefinitionsCommand, "nextToken", "nextToken", "");

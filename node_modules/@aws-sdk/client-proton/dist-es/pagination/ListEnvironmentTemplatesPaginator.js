import { createPaginator } from "@smithy/core";
import { ListEnvironmentTemplatesCommand, } from "../commands/ListEnvironmentTemplatesCommand";
import { ProtonClient } from "../ProtonClient";
export const paginateListEnvironmentTemplates = createPaginator(ProtonClient, ListEnvironmentTemplatesCommand, "nextToken", "nextToken", "maxResults");

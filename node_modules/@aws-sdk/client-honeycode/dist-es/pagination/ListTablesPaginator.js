import { createPaginator } from "@smithy/core";
import { ListTablesCommand } from "../commands/ListTablesCommand";
import { HoneycodeClient } from "../HoneycodeClient";
export const paginateListTables = createPaginator(HoneycodeClient, ListTablesCommand, "nextToken", "nextToken", "maxResults");

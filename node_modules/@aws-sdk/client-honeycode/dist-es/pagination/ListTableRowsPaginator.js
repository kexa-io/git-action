import { createPaginator } from "@smithy/core";
import { ListTableRowsCommand, } from "../commands/ListTableRowsCommand";
import { HoneycodeClient } from "../HoneycodeClient";
export const paginateListTableRows = createPaginator(HoneycodeClient, ListTableRowsCommand, "nextToken", "nextToken", "maxResults");

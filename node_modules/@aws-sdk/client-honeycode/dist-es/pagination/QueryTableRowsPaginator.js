import { createPaginator } from "@smithy/core";
import { QueryTableRowsCommand, } from "../commands/QueryTableRowsCommand";
import { HoneycodeClient } from "../HoneycodeClient";
export const paginateQueryTableRows = createPaginator(HoneycodeClient, QueryTableRowsCommand, "nextToken", "nextToken", "maxResults");

import { createPaginator } from "@smithy/core";
import { ListContactFlowModulesCommand, } from "../commands/ListContactFlowModulesCommand";
import { ConnectClient } from "../ConnectClient";
export const paginateListContactFlowModules = createPaginator(ConnectClient, ListContactFlowModulesCommand, "NextToken", "NextToken", "MaxResults");

import { createPaginator } from "@smithy/core";
import { ListApprovedOriginsCommand, } from "../commands/ListApprovedOriginsCommand";
import { ConnectClient } from "../ConnectClient";
export const paginateListApprovedOrigins = createPaginator(ConnectClient, ListApprovedOriginsCommand, "NextToken", "NextToken", "MaxResults");

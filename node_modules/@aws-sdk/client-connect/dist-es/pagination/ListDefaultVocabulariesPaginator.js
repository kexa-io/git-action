import { createPaginator } from "@smithy/core";
import { ListDefaultVocabulariesCommand, } from "../commands/ListDefaultVocabulariesCommand";
import { ConnectClient } from "../ConnectClient";
export const paginateListDefaultVocabularies = createPaginator(ConnectClient, ListDefaultVocabulariesCommand, "NextToken", "NextToken", "MaxResults");

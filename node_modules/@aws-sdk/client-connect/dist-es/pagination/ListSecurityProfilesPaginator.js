import { createPaginator } from "@smithy/core";
import { ListSecurityProfilesCommand, } from "../commands/ListSecurityProfilesCommand";
import { ConnectClient } from "../ConnectClient";
export const paginateListSecurityProfiles = createPaginator(ConnectClient, ListSecurityProfilesCommand, "NextToken", "NextToken", "MaxResults");

import { createPaginator } from "@smithy/core";
import { ListRoutingProfilesCommand, } from "../commands/ListRoutingProfilesCommand";
import { ConnectClient } from "../ConnectClient";
export const paginateListRoutingProfiles = createPaginator(ConnectClient, ListRoutingProfilesCommand, "NextToken", "NextToken", "MaxResults");

import { createPaginator } from "@smithy/core";
import { SearchRoutingProfilesCommand, } from "../commands/SearchRoutingProfilesCommand";
import { ConnectClient } from "../ConnectClient";
export const paginateSearchRoutingProfiles = createPaginator(ConnectClient, SearchRoutingProfilesCommand, "NextToken", "NextToken", "MaxResults");

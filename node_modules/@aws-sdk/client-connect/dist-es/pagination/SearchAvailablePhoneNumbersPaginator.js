import { createPaginator } from "@smithy/core";
import { SearchAvailablePhoneNumbersCommand, } from "../commands/SearchAvailablePhoneNumbersCommand";
import { ConnectClient } from "../ConnectClient";
export const paginateSearchAvailablePhoneNumbers = createPaginator(ConnectClient, SearchAvailablePhoneNumbersCommand, "NextToken", "NextToken", "MaxResults");

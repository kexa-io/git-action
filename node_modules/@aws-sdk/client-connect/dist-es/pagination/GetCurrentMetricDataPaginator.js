import { createPaginator } from "@smithy/core";
import { GetCurrentMetricDataCommand, } from "../commands/GetCurrentMetricDataCommand";
import { ConnectClient } from "../ConnectClient";
export const paginateGetCurrentMetricData = createPaginator(ConnectClient, GetCurrentMetricDataCommand, "NextToken", "NextToken", "MaxResults");

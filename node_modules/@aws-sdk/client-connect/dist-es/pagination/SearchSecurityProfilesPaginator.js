import { createPaginator } from "@smithy/core";
import { SearchSecurityProfilesCommand, } from "../commands/SearchSecurityProfilesCommand";
import { ConnectClient } from "../ConnectClient";
export const paginateSearchSecurityProfiles = createPaginator(ConnectClient, SearchSecurityProfilesCommand, "NextToken", "NextToken", "MaxResults");
